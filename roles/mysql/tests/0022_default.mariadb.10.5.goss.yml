---

package:
  mariadb-server:
    installed: true
  mariadb-client:
    installed: true

command:
  mysql --version:
    exit-status: 0
    stdout:
      - "/mysql(.)*Distrib 10.5.(.){1,2}-MariaDB/"
  mysqladmin  --user=admin --password=bTxKGlBi ping:
    exit-status: 0
    stdout:
      - "mysqld is alive"
  mysql -u root --execute "show databases":
    exit-status: 0
    stdout:
      - "manala_test_1"
      - "manala_test_1"
      - "!manala_test_3"
