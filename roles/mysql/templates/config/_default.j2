{%- set config = manala_mysql_config -%}

{%- if config is iterable and config is not string -%}

{#- Deprecated -#}
{%- import '_macros.j2' as macros with context -%}

{{ macros.config(config) }}

{%- else -%}

{{ config }}

{%- endif -%}
